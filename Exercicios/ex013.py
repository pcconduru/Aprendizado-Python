#Reajuste Salárial
salário = float(input('Qual o salário do funcionário: R$'))
aumento = (salário * 15) / 100
print('O sálario do funcionário que é de R${} mais o aumento dos 15% se tornará R${}'.format(salário, salário+aumento))